---
phase: 02-data-pipeline
plan: 07
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - frontend/src/components/PromptInput.tsx
  - frontend/src/components/SourceChip.tsx
  - frontend/src/components/UploadDropzone.tsx
  - frontend/src/components/ColumnMappingTable.tsx
  - frontend/src/components/FrequencyMismatchDialog.tsx
  - frontend/src/components/QuickDetailedToggle.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-09
  - DATA-12
  - DATA-13
  - DATA-14

must_haves:
  truths:
    - "PromptInput is a multi-line textarea with submit button"
    - "SourceChip renders detected source with edit popover and error state"
    - "UploadDropzone accepts drag-and-drop CSV/Excel/JSON with visual feedback"
    - "ColumnMappingTable shows auto-detected columns with type/role dropdowns"
    - "FrequencyMismatchDialog is a blocking modal with radio options"
    - "QuickDetailedToggle persists mode to Zustand store"
  artifacts:
    - path: "frontend/src/components/PromptInput.tsx"
      provides: "Prompt textarea component"
      contains: "PromptInput"
    - path: "frontend/src/components/SourceChip.tsx"
      provides: "Editable source chip"
      contains: "SourceChip"
    - path: "frontend/src/components/UploadDropzone.tsx"
      provides: "File upload drag-and-drop area"
      contains: "UploadDropzone"
    - path: "frontend/src/components/FrequencyMismatchDialog.tsx"
      provides: "Blocking frequency conflict dialog"
      contains: "FrequencyMismatchDialog"
  key_links:
    - from: "frontend/src/components/SourceChip.tsx"
      to: "frontend/src/types/data.ts"
      via: "Uses ParsedSource type"
      pattern: "import.*ParsedSource.*from.*types/data"
    - from: "frontend/src/components/FrequencyMismatchDialog.tsx"
      to: "frontend/src/types/data.ts"
      via: "Uses FrequencyConflict type"
      pattern: "import.*FrequencyConflict.*from.*types/data"
---

<objective>
Build all Phase 2 React UI components: PromptInput, SourceChip, UploadDropzone, ColumnMappingTable, FrequencyMismatchDialog, and QuickDetailedToggle. These are standalone components -- workspace page wiring happens in Plan 08.

Purpose: UI components implement the visual/interaction contracts from 02-UI-SPEC.md and consume the types defined in Plan 03.
Output: 6 React components ready for integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-UI-SPEC.md
@frontend/src/types/data.ts
@frontend/src/store/analysis.ts
@frontend/src/components/ui/button.tsx
@frontend/src/components/JobStatusCard.tsx
@frontend/src/lib/api.ts

<interfaces>
From frontend/src/types/data.ts (created in Plan 03):
```typescript
export interface ParsedSource {
  source: "FRED" | "YAHOO";
  series_id: string;
  display_name: string;
  rationale: string;
  valid: boolean | null;
  suggestions: Array<{ id: string; name: string }>;
}

export interface FrequencyConflict {
  has_conflict: boolean;
  series_frequencies: FrequencyInfo[];
  recommendation: string;
  recommended_method: string;
  target_frequency: string;
}

export interface ColumnInfo {
  name: string;
  detected_type: "numeric" | "date" | "text" | "category";
  role: "dependent" | "independent" | "date_index" | "ignore" | null;
}

export interface UploadResult {
  preview: Record<string, unknown>[];
  columns: ColumnInfo[];
  changes: string[];
  total_rows: number;
}

export type AnalysisMode = "quick" | "detailed";
```

From frontend/src/store/analysis.ts (created in Plan 03):
```typescript
export const useAnalysisStore = create<AnalysisState>()(...)
// Key actions: setPrompt, setSources, updateSource, removeSource, setMode,
//              setFrequencyConflict, setResolution, setUploadResult
```

From frontend/src/lib/api.ts:
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PromptInput, SourceChip, and QuickDetailedToggle</name>
  <files>
    frontend/src/components/PromptInput.tsx,
    frontend/src/components/SourceChip.tsx,
    frontend/src/components/QuickDetailedToggle.tsx
  </files>
  <read_first>
    frontend/src/types/data.ts,
    frontend/src/store/analysis.ts,
    frontend/src/components/ui/textarea.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/popover.tsx,
    frontend/src/components/ui/tabs.tsx,
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/input.tsx,
    .planning/phases/02-data-pipeline/02-UI-SPEC.md
  </read_first>
  <action>
    1. Create frontend/src/components/PromptInput.tsx:
       - Props: `onSubmit: (prompt: string) => void`, `disabled?: boolean`
       - Uses shadcn Textarea (auto-grows via rows + CSS resize-none)
       - Submit button with label "Fetch Data" (per Copywriting Contract), primary accent fill
       - Disabled state during fetch (disabled prop controls both textarea and button)
       - Placeholder: "Describe what you want to analyse..." in --muted-foreground
       - Reads prompt from useAnalysisStore, calls setPrompt on change
       - On submit: calls onSubmit(prompt) -- validation: min 5 chars
       - Layout: Textarea full-width, button right-aligned below
       - Styling per UI-SPEC: background --card (#1e293b), border --border (#334155), text --foreground (#f1f5f9), focus ring --accent (#6366f1)

    2. Create frontend/src/components/SourceChip.tsx:
       - Props: `source: ParsedSource`, `index: number`, `onOverride: (index: number, newSeriesId: string) => void`, `onRemove: (index: number) => void`
       - Anatomy per UI-SPEC: icon (Lucide `Database` for FRED, `TrendingUp` for YAHOO) + source label + series_id + X remove button
       - Uses shadcn Badge as base + shadcn Popover for edit state
       - Default state: border --border, text --foreground, background --card
       - Edit state (click to open Popover): border --accent (#6366f1), Popover with Input pre-filled with series_id and "Confirm" Button
       - Error state (valid === false): border --destructive (#ef4444), Badge overlay "Invalid -- click to fix"
       - Suggestions: if source.suggestions.length > 0, show "Did you mean:" list in Popover below the input
       - X remove button: onClick calls onRemove(index)
       - Confirm in Popover: calls onOverride(index, newValue)
       - Animations: 150ms fade-in on mount (CSS transition opacity), 100ms fade-out on remove (use state + setTimeout)
       - Touch target: minimum 44px height per UI-SPEC

    3. Create frontend/src/components/QuickDetailedToggle.tsx:
       - Uses shadcn Tabs with two items: "Quick" and "Detailed"
       - Reads mode from useAnalysisStore, calls setMode on change
       - Active side: --accent (#6366f1) indicator. Inactive: --muted-foreground text
       - Small size: text-xs or sm variant
       - Left-aligned positioning (parent handles layout)
       - No icons needed per UI-SPEC
       - State persists automatically via Zustand persist middleware (store handles localStorage)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PromptInput, SourceChip (with edit/error states), and QuickDetailedToggle implemented per UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2: Build UploadDropzone, ColumnMappingTable, and FrequencyMismatchDialog</name>
  <files>
    frontend/src/components/UploadDropzone.tsx,
    frontend/src/components/ColumnMappingTable.tsx,
    frontend/src/components/FrequencyMismatchDialog.tsx
  </files>
  <read_first>
    frontend/src/types/data.ts,
    frontend/src/store/analysis.ts,
    frontend/src/components/ui/dialog.tsx,
    frontend/src/components/ui/radio-group.tsx,
    frontend/src/components/ui/table.tsx,
    frontend/src/components/ui/collapsible.tsx,
    frontend/src/lib/api.ts,
    .planning/phases/02-data-pipeline/02-UI-SPEC.md
  </read_first>
  <action>
    1. Create frontend/src/components/UploadDropzone.tsx:
       - Props: `onUpload: (result: UploadResult) => void`
       - Implements drag-and-drop via native onDragOver/onDragEnter/onDragLeave/onDrop events + hidden file input for click-to-browse
       - Default state: dashed border --border, background --card, centered text "Drop CSV, Excel, or JSON here -- or click to browse" in --muted-foreground
       - Drag-over state: border --accent (#6366f1), background #1e3a5f (inline style)
       - Accepted extensions: .csv, .xlsx, .xls, .json
       - On file drop/select: POST to /data/upload as multipart form data using `fetch` with FormData (not apiFetch which sets Content-Type: application/json)
       - Loading state: if parse >500ms, show Lucide `Loader2` spinning at 20px centered
       - File rejected (wrong type): shake animation (add animate-shake class for 500ms), error text "Could not read this file. Make sure it is a valid CSV, Excel, or JSON file." in --destructive for 4 seconds
       - On success: calls onUpload(result) which will trigger ColumnMappingTable rendering in parent

    2. Create frontend/src/components/ColumnMappingTable.tsx:
       - Props: `result: UploadResult`, `onConfirm: (columns: ColumnMapping[]) => void`
       - Uses shadcn Table for the preview rows (first 5-10 rows)
       - Column headers: column name (bold, 14px), with type dropdown (Select or native select: numeric/date/text/category) and role dropdown (dependent/independent/date_index/ignore)
       - Local state: array of ColumnMapping objects initialized from result.columns
       - Rows with type coercion issues: yellow dot (#f59e0b) in column header (check if column name appears in result.changes)
       - "Confirm Mapping" button: primary accent fill, disabled until all columns have a role assigned or set to "ignore"
       - Auto-fix report: if result.changes.length > 0, show Collapsible above table with text from Copywriting Contract: "{N} rows dropped, {M} values coerced -- click to see details"
       - onClick confirm: calls onConfirm with current column mappings

    3. Create frontend/src/components/FrequencyMismatchDialog.tsx:
       - Props: `conflict: FrequencyConflict`, `onConfirm: (choice: ResolutionChoice) => void`, `onCancel: () => void`
       - Uses shadcn Dialog (always open when rendered -- no internal open state)
       - `onInteractOutside={(e) => e.preventDefault()}` -- blocking, no outside click dismiss (D-07)
       - No X close button -- per UI-SPEC
       - Heading: "Data frequencies do not match" (20px, semibold) per Copywriting Contract
       - Body: template from Copywriting Contract: "{Source A}: {N} {freq} rows . {Source B}: {M} {freq} rows -- choose how to align them before proceeding."
       - Recommendation card: slightly lighter background than --card, border --accent, text: "Recommended: {conflict.recommendation}"
       - Options as shadcn RadioGroup:
         - Aggregate to lower frequency (mean) -- pre-selected if recommended_method is "mean"
         - Aggregate to lower frequency (last)
         - Aggregate to lower frequency (sum)
         - Interpolate to higher frequency (forward-fill)
         - Abort analysis
       - Footer: "Confirm" button (primary, accent fill) and "Cancel analysis" (ghost button)
       - On confirm: builds ResolutionChoice from selected radio value and calls onConfirm
       - On cancel/abort: calls onCancel
       - Local state: selected method (initialized to conflict.recommended_method)
       - Import types: FrequencyConflict, ResolutionChoice from @/types/data
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>UploadDropzone with drag-and-drop, ColumnMappingTable with type/role editing, FrequencyMismatchDialog as blocking modal -- all per UI-SPEC</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- All 6 component files exist and export named components
- FrequencyMismatchDialog has onInteractOutside preventDefault (blocking requirement D-07)
</verification>

<success_criteria>
All 6 Phase 2 UI components built per UI-SPEC interaction contracts, using shadcn primitives, consuming types from data.ts. Each component is self-contained and ready for WorkspacePage integration in Plan 08.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-07-SUMMARY.md`
</output>
