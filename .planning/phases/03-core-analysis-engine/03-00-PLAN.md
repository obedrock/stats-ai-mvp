---
phase: 03-core-analysis-engine
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_r_template.py
  - backend/tests/test_code_gen.py
  - backend/tests/test_r_execution.py
  - backend/tests/test_interpretation.py
  - frontend/src/components/__tests__/ResultsPanel.test.tsx
autonomous: true
requirements:
  - ANAL-01
  - ANAL-05
  - ANAL-06
  - ANAL-07
  - ANAL-08
  - RSLT-01
  - RSLT-02
  - RSLT-03
  - RSLT-04
  - RSLT-05
  - RSLT-06

must_haves:
  truths:
    - "All 5 test stub files exist and are importable by their respective test runners"
    - "Backend stubs use pytest.mark.skip with 'Wave 0 stub' reason so they show as skipped, not failures"
    - "Frontend stub uses vitest it.skip so it shows as skipped, not a failure"
    - "Running pytest and vitest succeeds (exit 0) with skipped tests reported"
  artifacts:
    - path: "backend/tests/test_r_template.py"
      provides: "Skipped test stubs for OLS template output validation"
      contains: "pytest.mark.skip"
    - path: "backend/tests/test_code_gen.py"
      provides: "Skipped test stubs for Claude prompt-to-R code generation"
      contains: "pytest.mark.skip"
    - path: "backend/tests/test_r_execution.py"
      provides: "Skipped test stubs for R subprocess execution and error handling"
      contains: "pytest.mark.skip"
    - path: "backend/tests/test_interpretation.py"
      provides: "Skipped test stubs for result interpretation and follow-ups"
      contains: "pytest.mark.skip"
    - path: "frontend/src/components/__tests__/ResultsPanel.test.tsx"
      provides: "Skipped test stubs for frontend results rendering"
      contains: "it.skip"
  key_links: []
---

<objective>
Create test stub files that Plans 01-04 will make pass. These are Wave 0 Nyquist scaffolds: they define what must be tested without implementing the tests or the code under test.

Purpose: Satisfies Nyquist compliance — every plan's verify command can reference a real test file. Stubs are skipped so the test suite stays green until implementations land.
Output: 4 backend pytest files + 1 frontend vitest file, all with skipped test functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-analysis-engine/03-CONTEXT.md
@.planning/phases/03-core-analysis-engine/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend test stubs for Plans 01-03</name>
  <files>backend/tests/test_r_template.py, backend/tests/test_code_gen.py, backend/tests/test_r_execution.py, backend/tests/test_interpretation.py</files>
  <read_first>
    - backend/tests/conftest.py (existing conftest pattern)
    - backend/tests/test_r_sandbox.py (existing R-related test pattern)
    - .planning/phases/03-core-analysis-engine/03-VALIDATION.md (test map with requirement IDs)
  </read_first>
  <action>
Create 4 backend test stub files. Each test function is decorated with `@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan NN")` so the suite stays green.

**1. `backend/tests/test_r_template.py`** — Stubs for Plan 01 (R template + schemas):

```python
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01")
def test_ols_template_produces_valid_json():
    """R template with filled slots produces JSON with coefficients, diagnostics, model_summary, plotly_charts keys."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01")
def test_ols_template_diagnostics_section():
    """R template JSON contains breusch_pagan, durbin_watson, vif, shapiro_wilk in diagnostics."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01")
def test_pydantic_schemas_validate_r_output():
    """AnalysisResultResponse schema accepts the R template's JSON structure."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01")
def test_typescript_types_match_pydantic_fields():
    """TypeScript type field names match Pydantic schema field names (checked via regex on both files)."""
    pass
```

**2. `backend/tests/test_code_gen.py`** — Stubs for Plan 02 (Claude services):

```python
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_generate_ols_slots_returns_required_keys():
    """generate_ols_slots returns dict with dep_var, indep_vars, transformations."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_generate_ols_slots_uses_tool_use():
    """generate_ols_slots calls Claude with tool_choice type=tool."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_render_ols_script_fills_all_slots():
    """render_ols_script replaces all 4 template slots: DATA_PATH, DEP_VAR, INDEP_VARS, TRANSFORMATIONS."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_interpret_ols_results_returns_interpretation_and_suggestions():
    """interpret_ols_results returns dict with interpretation and follow_up_suggestions."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_explain_r_error_returns_explanation_and_prompt():
    """explain_r_error returns dict with error_explanation and suggested_prompt."""
    pass
```

**3. `backend/tests/test_r_execution.py`** — Stubs for Plan 03 (Celery task + API):

```python
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 03")
def test_run_ols_analysis_task_success_path():
    """run_ols_analysis task completes and updates Job with status=success, r_result_json, interpretation."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 03")
def test_run_ols_analysis_task_error_path():
    """run_ols_analysis task handles R failure, updates Job with status=error, error_explanation."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 03")
def test_analysis_run_endpoint_starts_celery_task():
    """POST /analysis/run returns 200 with job_id and status=running."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 03")
def test_analysis_poll_endpoint_returns_results():
    """GET /analysis/{job_id} returns full AnalysisResultResponse when job is complete."""
    pass
```

**4. `backend/tests/test_interpretation.py`** — Stubs for Plans 02-03 interpretation paths:

```python
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02/03")
def test_interpretation_contains_coefficient_references():
    """Interpretation text references specific coefficient values and p-values."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02/03")
def test_coefficient_table_has_all_fields():
    """Coefficient rows contain variable, estimate, std_error, t_stat, p_value, ci_lower, ci_upper."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02/03")
def test_diagnostics_bundle_complete():
    """Diagnostics contain breusch_pagan, durbin_watson, vif, shapiro_wilk results."""
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02/03")
def test_follow_up_suggestions_are_context_aware():
    """Follow-up suggestions reference diagnostic failures from the results."""
    pass
```

All files use `import pytest` only. No application imports (those don't exist yet).
  </action>
  <verify>
    <automated>cd /c/Users/obedr/stats-ai-mvp/backend && .venv/Scripts/python.exe -m pytest tests/test_r_template.py tests/test_code_gen.py tests/test_r_execution.py tests/test_interpretation.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 backend test files exist, all tests show as SKIPPED (not failed), pytest exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend test stubs for Plan 04</name>
  <files>frontend/src/components/__tests__/ResultsPanel.test.tsx</files>
  <read_first>
    - frontend/vitest.config.ts (vitest configuration — test file pattern, setup)
    - frontend/package.json (check vitest is installed)
  </read_first>
  <action>
Create `frontend/src/components/__tests__/` directory and `ResultsPanel.test.tsx` with skipped vitest stubs.

```tsx
import { describe, it } from "vitest";

describe("ResultsPanel", () => {
  it.skip("renders InterpretationSection with text prop", () => {
    // Wave 0 stub — implementation in Plan 04
  });

  it.skip("renders CoefficientTable with coefficients and modelSummary props", () => {
    // Wave 0 stub — implementation in Plan 04
  });

  it.skip("renders DiagnosticsRow with correct PASS/WARN/FAIL badges", () => {
    // Wave 0 stub — implementation in Plan 04
  });

  it.skip("renders ChartGrid with Plotly charts", () => {
    // Wave 0 stub — implementation in Plan 04
  });

  it.skip("renders RCodeBlock collapsed by default with copy button", () => {
    // Wave 0 stub — implementation in Plan 04
  });

  it.skip("renders ErrorResultCard with error explanation and retry button", () => {
    // Wave 0 stub — implementation in Plan 04
  });

  it.skip("renders FollowUpRow with clickable suggestion chips", () => {
    // Wave 0 stub — implementation in Plan 04
  });
});
```

Ensure the `__tests__` directory is created first: `mkdir -p frontend/src/components/__tests__`.
  </action>
  <verify>
    <automated>cd /c/Users/obedr/stats-ai-mvp/frontend && npx vitest run --reporter=verbose src/components/__tests__/ResultsPanel.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>Frontend test stub file exists, all 7 tests show as skipped, vitest exits 0.</done>
</task>

</tasks>

<verification>
- All 5 test files exist: `ls backend/tests/test_r_template.py backend/tests/test_code_gen.py backend/tests/test_r_execution.py backend/tests/test_interpretation.py frontend/src/components/__tests__/ResultsPanel.test.tsx`
- Backend tests pass with skips: `cd backend && .venv/Scripts/python.exe -m pytest tests/test_r_template.py tests/test_code_gen.py tests/test_r_execution.py tests/test_interpretation.py -v`
- Frontend tests pass with skips: `cd frontend && npx vitest run src/components/__tests__/ResultsPanel.test.tsx`
</verification>

<success_criteria>
All test stub files exist and their respective test runners report them as skipped (not failed). The test suite stays green. Plans 01-04 can now reference these test files in their verify commands.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-analysis-engine/03-00-SUMMARY.md`
</output>
